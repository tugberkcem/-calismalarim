#include <stdio.h>
#include <stdlib.h>
int main() {

	int number;
	int sonuc;
	scanf("%d",&sonuc);
	scanf("%d",&number);
	sonuc==0? number>0? sonuc=number<5? 1:0:0:0; 
	printf("%d",sonuc);
return 0;
}
